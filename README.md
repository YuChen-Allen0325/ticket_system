# E-Commerce Backend Architecture Demo

This repository demonstrates a scalable backend architecture:

- **Client-facing API**: FastAPI + PostgreSQL + Redis + S3
- **Internal admin/backend**: Django REST Framework + PostgreSQL + Redis + S3

<br>All service deploy by AWS
